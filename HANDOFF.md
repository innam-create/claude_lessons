# Hand-off: ZX Spectrum — стан проєкту

**Проєкт:** освітній розділ ZX Spectrum для Software and Computer Museum
(`spectrum.sncmuseum.org`)
**Стан:** розділ **Моделі** (`/models/` + картки моделей) закритий повністю за ТЗ §5.2 —
фільтри, компаратор із таблицею відмінностей, картка моделі. Решта розділів сайту
ще не існує.
**Оновлено:** 2026-07-17

> Цей документ описує **поточний стан**, а не історію змін. Історія — у `git log`,
> кожен коміт містить розгорнуте пояснення рішень.

---

## 0. TL;DR — почати за 30 секунд

```bash
npm install
npm run dev     # → http://localhost:4321/models/   і   /en/models/
```

Перед комітом:

```bash
npm run check:licenses   # ліцензії зображень (ТЗ §7.2)
npm run check            # типи + розмітка
npm run build            # 24 сторінки
```

Далі читати: **§1** (джерела істини) → **§7** (що робити далі) → **§8** (пастки).

---

## 1. Контекст і джерела істини

Порядок пріоритету (за розбіжностей — вищий переважає):

1. **`TZ_spectrum_sncmuseum.md`** — технічне завдання замовника. *Головний документ вимог.*
2. **`project/*.dc.html`** — дизайн-прототипи з Claude Design.
3. **`project/design_handoff_zx_spectrum/README.md`** — дизайн-токени (hex, типографіка).
4. **`chats/`** — стенограми дизайн-сесій (чому саме такі рішення).

> ⚠️ **ТЗ і дизайн подекуди суперечать одне одному, і це не помилка читання.**
> Знайдені розбіжності описані там, де вони стосуються справи: §7.3 (перелік клонів),
> §7.4 (чого немає в макетах). Коли макет суперечить вимозі ТЗ — виграє ТЗ, але
> **рішення фіксувати в коментарі й коміті**, а не тихо.

> ⚠️ **`project/assets/IMAGE-CREDITS.md` — ненадійний.** Реєстр зображень із бандла
> помилявся **тричі з чотирьох перевірок**: приписував авторство «(Wikimedia)» замість
> реальних людей, а `zx-plus3.jpg` — не того автора й не ту версію ліцензії.
> **Не використовувати як джерело ліцензійних даних.** Джерело істини —
> `src/data/models.json`. Кожне фото звіряти на сторінці файлу Wikimedia (ТЗ §7.1).
> Ще не звірені й знадобляться для лонгрідів: `zx-board.jpg`, `cassette.jpg`.

**Правило доступності (тверде, з ТЗ §4.2):** контраст ≥ 4.5:1; `#0000FF` (blue) і
`#00FF00` (green) **ніколи не текст на чорному** — лише заливка/рамка/іконка.
**Колір ніколи не єдиний носій сенсу** — поряд завжди має бути підпис, знак або форма.

---

## 2. Стек

| Пункт | Рішення | Примітка |
|---|---|---|
| Фреймворк | **Astro 5.18.2** | ⚠️ ТЗ §3 вказує Astro 7 — його ще немає в npm. Код написано під архітектуру 7; оновлення має бути drop-in. Див. §6. |
| Node.js | локально **v24.18** | `.nvmrc` фіксує `22.12`. Обидві ≥ 22.12 (вимога ТЗ §3), але значення різні — `nvm use` поставить 22.12. CI бере версію з `.nvmrc`. |
| Мова | TypeScript (strict) | `npm run check` = 0 помилок |
| Контент | Content Collections + Zod | `src/content.config.ts` валідує `src/data/models.json` на збірці |
| i18n | вбудований Astro i18n | `uk` без префікса, `en` під `/en/` |
| Інтерактив | один vanilla-острів | фільтри + компаратор; без JS-фреймворку |
| Зображення | `astro:assets` | авто-WebP; 5 файлів × 5 розмірів у білді |
| Стилі | scoped CSS + токени в `theme.css` | без CSS-фреймворку |

---

## 3. Структура

```
├── .github/
│   ├── workflows/ci.yml          check:licenses → check → build на кожен PR
│   └── pull_request_template.md  чек-лист ТЗ §2 (факти · джерела · ліцензії · затвердження)
├── .claude/launch.json           конфіг dev-сервера
├── scripts/check-licenses.ts     перевірка ліцензій (ТЗ §7.2), запускається в CI
│
├── src/
│   ├── content.config.ts   схема моделей (Zod) — enum ліцензій будується з lib/licenses.ts
│   ├── data/models.json    ← ДАНІ 10 моделей. Головне місце для контент-правок
│   ├── i18n/ui.ts          усі рядки UI UA/EN + localizePath()/useTranslations()
│   │
│   ├── lib/
│   │   ├── licenses.ts     ← LICENSES (єдине джерело істини для enum і CI),
│   │   │                     deriveCommercialUse(), назви/URL ліцензій, ALLOWED_SOURCES
│   │   ├── catalog.ts      normalizeModels(), buildSpecs(), buildFilterGroups()
│   │   └── images.ts       мапа ім'я файлу → оптимізоване зображення
│   │
│   ├── styles/theme.css    ТОКЕНИ: палітра ZX, музейні navy/gold, типографіка, CRT
│   ├── layouts/BaseLayout.astro   <head>, шрифти, hreflang, CRT без спалаху
│   │
│   ├── components/
│   │   ├── Header.astro        меню, UA/EN, CRT, бургер (< 1280px — див. §8)
│   │   ├── Footer.astro        музейний футер
│   │   ├── Breadcrumbs.astro   спільні крихти (ТЗ §5.1)
│   │   ├── ModelCard.astro     картка каталогу + кнопка «+ порівняти»
│   │   ├── ModelDetail.astro   картка моделі: hero, характеристики, галерея, джерела
│   │   ├── ImageCredit.astro   «автор · ліцензія · джерело» з посиланнями (ТЗ §7.3)
│   │   ├── CatalogView.astro   каталог: фільтри, сітка, компаратор + таблиця
│   │   └── RainbowBar.astro    смуга Sinclair
│   │
│   ├── assets/models/      zx-48k · zx-128 · zx-plus · zx-plus3 · zx81
│   └── pages/
│       ├── index.astro              головна UA — ЗАГЛУШКА (§7.2)
│       ├── models/index.astro       каталог UA
│       ├── models/[slug].astro      картка моделі UA
│       └── en/…                     дзеркало EN
│
├── project/                дизайн-прототипи (референс, не збирається)
└── chats/                  стенограми дизайн-сесій
```

---

## 4. Дизайн-система

Усі значення — у `src/styles/theme.css`. Використовувати змінні, не хардкодити.

**Колір:** `--bg:#000` · `--text:#D7D7D7` · `--text-dim:#A8A8A8`.
ZX BRIGHT: `--zx-red`, `--zx-magenta`, `--zx-cyan`, `--zx-yellow`, `--zx-white` — можна текстом;
`--zx-blue`, `--zx-green` — **лише заливка/рамка**.
Музей: `--museum-navy/#152A52`, `--museum-gold/#FFD700`.
Маджента — колір компаратора (панель, чипси, таблиця).

**Типографіка:** `--font-pixel` (Retroville, заголовки ≥16px, UPPERCASE) ·
`--font-body` (Golos Text, ≥18px) · `--font-mono` (IBM Plex Mono, метадані).

**Форма:** кути 0; елевація рамками, не тінями; сітка ~1180px. Рух 160–240ms;
поважати `prefers-reduced-motion`. Фокус — видима 2px рамка.

**CRT:** `html.crt`, стан у `localStorage['zx-crt']`, накладка в `BaseLayout`.

---

## 5. Модель даних

`src/data/models.json` — масив, валідований `src/content.config.ts`:

```jsonc
{
  "id": "zx-spectrum-48k",          // = slug, формує /models/{id}/
  "title": { "uk": "…", "en": "…" },
  "manufacturer": "Sinclair",        // коротка назва → чипс фільтра
  "manufacturerFull": "Sinclair Research",  // повна → рядок «Виробник» у таблиці
  "country": "GB",                   // enum GB|PT|US|PL
  "year": "1982",
  "ram": "48 KB",
  "ramKb": 48,
  "desc": { "uk": "…", "en": "…" },      // короткий — картка каталогу
  "summary": { "uk": "…", "en": "…" },   // розгорнутий — hero картки моделі
  "cpu": "Zilog Z80A", "cpuClockMhz": 3.5, "romKb": 16,
  "video": { "uk": "…", "en": "…" },
  "sound": { "uk": "…", "en": "…" },
  "storage": { "uk": "…", "en": "…" },
  "keyboard": null,                  // null = НЕВІДОМО → «невідомо» курсивом
  "massG": null,
  "sources": [],                     // [] → блок «Джерела ще не додано»
  "images": [                        // [] → «Шукаємо фотографію»; [0] — обкладинка
    {
      "src": "zx-48k.jpg",
      "alt": { "uk": "…", "en": "…" },   // ТЗ §10: описувати, що ВИДНО
      "license": "CC-BY-SA-2.5",         // enum з src/lib/licenses.ts
      "author": "Bill Bertram",
      "source_url": "https://commons.wikimedia.org/wiki/File:ZXSpectrum48k.jpg",
      "accessed": "2026-07-17"
    }
  ],
  "inMuseum": true,
  "confidence": "high"               // low → видимий бейдж «⚠ уточнення» (ТЗ §2)
}
```

**`null` = «невідомо», а не «немає».** Різниця змістовна: у ZX80 звуку **немає**, і це
записано як `{"uk":"Немає","en":"None"}`, а не `null` — інакше таблиця сказала б
«невідомо», що неправда. Не плутати.

**Ліцензійні поля — тверде правило (ТЗ §7.2).** `license`, `author`, `source_url`,
`accessed` обов'язкові. Дві лінії захисту:

1. **Zod** — валить `astro build`, якщо поля бракує.
2. **`npm run check:licenses`** — ловить те, чого схема не бачить: файл відсутній на
   диску, файл-сирота без задокументованої ліцензії, джерело поза ієрархією ТЗ §7.1,
   `accessed` із майбутнього чи неіснуюча дата. Читабельний звіт замість стектрейсу
   Zod. У CI — **перед** збіркою.

**Фільтри виводяться з даних самі** (`buildFilterGroups`): додаси модель — чипси
оновляться. `commercial_use` не пишеться руками — виводиться з `license` (ТЗ §14).

---

## 6. Astro 5 → 7

Коли вийде Astro 7: `astro@^7`, Node лишається ≥ 22.12. Content Collections,
`file()`-loader та i18n у коді вже в «новому» стилі. Rust-компілятор суворіший до
розмітки — прогнати `npm run build`. Remark/rehype-плагінів немає.

> Через це в коді **немає жодного вкладеного `<a>`** — Astro 7 на цьому впаде (ризик R6).
> Це вплинуло на два рішення: плашка ліцензії в картці каталогу без посилань і
> «розтягнуте посилання» замість обгортки-`<a>` (§8).

---

## 7. Що далі

### 7.1. Заблоковане рішенням музею (не вгадувати)

| Питання | Суть |
|---|---|
| **Перелік клонів** | ТЗ §8.3 і `ClonesPage.dc.html` називають **різні** машини — збігаються 2 з 9. ТЗ: Timex 2068, TC2048, Ленінград-1, Пентагон-128, Байт, Дніпро, Компаньйон, Спектр-48, харківські збірки. Макет: Didaktik M, Didaktik Gama, Дубна 48К, Ленінград-1, Дельта-С, Пентагон-128. ТЗ саме себе називає чернеткою, «що потребує звірки з інвентарем», а §14 каже, що машиночитної бази немає. **Блокує весь розділ `/clones/`.** |
| **Верифікація характеристик** | Специфікації всіх 10 моделей — **чернетка без джерел**: узяті з `ModelDetailPage.dc.html` (8 моделей) або написані ШІ (ZX80/ZX81). `sources[]` порожні скрізь. ТЗ §12 (етап 4) вимагає ≥ 2 джерела на картку. ТЗ §2 дозволяє ШІ-чернетки, але публікацію гейтить людина. |
| **`inventory_id`** | ТЗ §14: поле `optional`, але за `inMuseum: true` CI має вимагати його наявність. У схемі поля **немає** — інвентарних номерів у репозиторії теж немає, їх дає музей. Зараз 3 моделі з `inMuseum: true`. |
| **Timex TC2048** | Лежить у `models.json`, хоча ТЗ §8.3 відносить його до клонів. Так показував дизайн каталогу. При появі `/clones/` — вирішити, чи переносити. |
| **Фото `zx-plus3.jpg`** | Знімок експоната на стіні: 4000×1828 (~2.2:1), темний. У hero 4:3 з `object-fit:contain` летербоксується у вузьку смугу й на чорному майже зникає. Верстка коректна — питання до фото (ТЗ R3): шукати заміну з чистою ліцензією або зняти власне. |

### 7.2. Вільне до роботи

1. **Головна** — зараз заглушка (напис, абзац, кнопка). Макет `HomePage.dc.html`
   має 4 секції: hero з фото, intro, 6 плиток розділів, «Нові матеріали» (3 лонгріди).
   ⚠️ 5 із 6 плиток вестимуть у 404, а «Нові матеріали» — це вигаданий контент
   дизайн-фази; лонгрідів немає. Робити, коли буде куди вести, або з чесними
   порожніми станами.
2. **`/peripherals/`** — Interface 1 + Microdrive, ZX Printer (частина MVP-20).
   Окрема колекція (ТЗ §6), макет `PeripheralsPage.dc.html`. **Не заблоковано.**
3. **Решта розділів:** HistoryHub + таймлайн, Longread ×5, Software, Exhibits,
   Contacts, Sources, About, 404 («R Tape loading error»). Макети — у `project/`.
4. **Компаратор — кнопка «лише відмінності».** Рядки `compare.diffOnly` уже є в
   `i18n/ui.ts`; лишилось відфільтрувати рядки в `buildTable()`.

### 7.3. Платформа / реліз (ТЗ §3, §9, §10)

5. **Remote і захист `main`** — репозиторію на GitHub **немає**, код існує в одному
   екземплярі на диску, поза бекапом. ТЗ §2 вимагає захист `main` і merge лише через
   PR після затвердження музеєм — це налаштовується після появи remote.
6. **CI ще жодного разу не проганявся** — воркфлоу написано, але без remote він не
   запускався. Перший PR може виявити помилки в ньому.
7. **Pagefind** (ТЗ §5.2), **SEO/OG**: sitemap, robots.txt, OG-зображення, RSS,
   Schema.org. `hreflang` уже є.
8. **Деплой:** Cloudflare Pages або GitHub Pages + Actions. DNS: CNAME `spectrum`.
9. **Редирект `/uk/* → /*`** (301) — на рівні хостингу.
10. **Аналітика** (Plausible) + Lighthouse ≥ 95/95.
11. **EN-контент** — перемикач працює, рядки UI перекладені. Для сторінок без
    перекладу знадобиться банер «сторінка ще не перекладена» (ТЗ R5).

### 7.4. Чого немає в макетах (доведеться проєктувати)

Дизайн не покриває все, що вимагає ТЗ. Уже спроєктовано з нуля в словнику
дизайн-системи: **таблиця відмінностей** компаратора, **кнопка «+ порівняти»** на
картці, **чесні порожні стани** («Джерела ще не додано»). Далі за макетами теж
будуть прогалини — тримати той самий підхід: словник дизайн-системи + запис рішення
в коміт.

---

## 8. Пастки (перевірено на власній шкурі)

**CSS Grid: завжди `minmax(0, 1fr)`, ніколи `1fr`.**
`1fr` = `minmax(auto, 1fr)`, а `auto`-мінімум = **min-content** вмісту. Рядок ліцензії
має `white-space:nowrap`, тож його min-content — уся довжина тексту (~338px): колонка
розпирала контейнер **зсередини**, і зміна ширини сітки не допомагала б.

**Піксельний шрифт не переноситься всередині слова.** «ХАРАКТЕРИСТИКИ» на 28px — це
347px у контейнері 288px. Текст малюється **поза** боксом, не розширюючи його: тому
`getBoundingClientRect()` показує норму, а елемент не потрапляє в жоден список
порушників. **Діагностувати тільки через `scrollWidth > clientWidth`.**

**Шапка вимагає ≥ 1280px.** Бургер вмикається на `max-width: 1279px` — це **виміряно**,
а не вгадано: меню з 7 пунктів + інструменти потребують ~1216px контенту. Збігається
з дизайном, де рівно два варіанти: desktop 1280 і mobile 390.
> **Брейкпоінти рахувати по англійській** — вона довша за українську (1246 проти 1185).

**Scoped-CSS Astro не доходить до елементів, створених JS.** Скоуп працює через
data-атрибут, який ставиться лише елементам із шаблону. Таблиця компаратора
рендерилась зовсім без стилів, а `.sr-only` показував службовий текст просто на
сторінці. Рішення — `:global()` під конкретним контейнером (`.cmp-table-wrap`).

**`localStorage` — лише CRT і мова (ТЗ §10).** Вибір моделей у компараторі живе тільки
в пам'яті й скидається при перезавантаженні. Це свідомо, не баг.

**Невідоме — не відмінність.** Компаратор рахує відмінність лише серед **відомих**
значень: «Мембранна · Мембранна · невідомо» — не «≠», бо ми не знаємо, чи значення
інше, і не маємо права це стверджувати. Той самий принцип — скрізь у контенті.

**Інше:**
- **«Мертві» пункти меню** (Історія/Клони/Софт/Периферія/Експонати/Контакти) ведуть
  у 404 — очікувано до збірки тих сторінок.
- **Хотлінки на Wikimedia не повертати** — усі фото локальні в `src/assets/`.
- **`project/` не збирається** Astro (поза `src/`), виключено в `tsconfig`.
- **Шрифти Google** локально й у проді вантажаться нормально. За бажанням —
  self-host Golos Text / IBM Plex Mono для швидкості й офлайну.

---

## 9. Перевірка якості

```bash
npm run check:licenses   # 6 зображень у 10 моделях, усі поля заповнені
npm run check            # 0 errors
npm run build            # 24 сторінки, Complete!
```

**Перевірено в браузері:** каталог (фільтри, лічильник, порожній стан), картка моделі
(hero, характеристики, порожні стани, prev/next), компаратор (ліміт 3, чипси,
очищення, таблиця, підсвітка відмінностей), UA + EN, адаптивність 320–1920
(7 сторінок × 10 ширин, 0 горизонтального скролу).

**Не перевірено:** закриття діалогу компаратора на `Esc` — панель браузера
перехоплювала клавішу, сторінка не отримувала подію. Для нативного `<dialog>` це
поведінка за замовчуванням, але власними очима не підтверджено. Кнопка «×» працює.

**Автотестів немає.** Playwright проти `npm run preview` — гарний наступний крок,
особливо для компаратора й адаптивності (критерій `scrollWidth === clientWidth`
перевіряється тривіально).

---

## 10. Стан Git

**8 комітів на `main`, remote немає.** Гілки злиті й видалені — історія лінійна:

```
d75ef9a  Компаратор: додавання з карток + таблиця відмінностей (ТЗ §5.2)
a0c1761  ZX80 і ZX81 у каталозі моделей (ТЗ §8.3)
295ee84  Адаптивність 320–1920 без горизонтального скролу (ТЗ §10)
5402568  Картка моделі /models/{slug}/ (ТЗ §5)
9bdd5ff  Структуровані ліцензії зображень + перевірка в CI (ТЗ §6/§7.2)
68b561e  Інфраструктура репозиторію за ТЗ §2
1807681  Каталог моделей /models/ на Astro (UA/EN)
9f78a16  Handoff-бандл з Claude Design + ТЗ замовника
```

Коміти навмисно розгорнуті — у них записані причини рішень і знайдені вади.
За розбіжностей із цим документом дивитись `git log`.

> ⚠️ **Прямі коміти в `main` — тимчасовий режим.** ТЗ §2 вимагає захищену гілку й
> merge лише через PR після затвердження музеєм. Поки немає remote, PR неможливий.
> **Після появи GitHub: увімкнути захист `main` і працювати через PR.**

**Ідентичність git** задана локально для цього репозиторію: `inna.m` /
`inna.m@sncmuseum.org` (глобальної не було). Змінити — `git config user.name "…"`.

---

## 11. Локальне середовище

Node **v24.18** встановлено, усе працює. `.nvmrc` фіксує `22.12` — обидві версії
задовольняють вимогу ТЗ §3 (≥ 22.12), але `nvm use` поставить саме 22.12; CI бере
версію з `.nvmrc`.

`npm install` показує попередження npm 11 про заблоковані postinstall-скрипти
(`sharp`, `esbuild`) — **на збірку не впливає**, оптимізація зображень працює.

---

## 12. Продовження з Claude Code

Дати агенту як контекст: **цей `HANDOFF.md`**, `TZ_spectrum_sncmuseum.md` і потрібний
прототип із `project/`.

Спільні компоненти й токени готові — нові сторінки мають **наслідувати** їх, а не
дублювати стилі. Breadcrumbs, ImageCredit, RainbowBar, BaseLayout, Header, Footer
перевикористовуються як є.

**Що варто сказати агенту прямо:**
- ТЗ переважає макет, але розбіжність фіксувати в коментарі й коміті.
- Не вигадувати факти й ліцензії: порожнє поле чесніше за здогад, для цього є
  `null`, порожні `sources[]`/`images[]` і чесні порожні стани.
- `IMAGE-CREDITS.md` не використовувати — звіряти по сторінці файлу Wikimedia.
- Перед комітом: `check:licenses` → `check` → `build`.
