# Hand-off: ZX Spectrum → локальна розробка

**Проєкт:** освітній розділ ZX Spectrum для Software and Computer Museum
(`spectrum.sncmuseum.org`)
**Що зроблено в цій сесії:** реалізовано сторінку **Каталог моделей** (`/models/`)
з усіма спільними компонентами, як робочий Astro-проєкт.
**Кому передається:** розробнику для продовження в локальному середовищі
(редактор + Node.js, за бажанням — Claude Code CLI).
**Дата:** 2026-07-15

---

## 0. TL;DR — з чого почати локально

```bash
# 1. Розпакувати архів проєкту (spectrum-sncmuseum.zip)
# 2. Переконатися, що встановлено Node.js ≥ 22.12  (nvm use — читає .nvmrc)
npm install
npm run dev
# 3. Відкрити http://localhost:4321/models/  (UA)  та  /en/models/  (EN)
```

Далі читати цей документ від розділу 3 (структура) і 7 (наступні кроки).

---

## 1. Контекст і джерела істини

Порядок пріоритету документів (за розбіжностей — вищий переважає):

1. **`TZ_spectrum_sncmuseum.md`** (технічне завдання замовника) — стек, i18n,
   модель даних, ліцензійні правила, нефункціональні вимоги, етапи. *Головний
   документ вимог.* (Якщо його немає в теці — це файл, що надсилався у чат;
   покласти поруч.)
2. **`project/*.dc.html`** — дизайн-прототипи (Design Components) з Claude Design.
   `CatalogPage.dc.html` — джерело візуалу для вже зробленої сторінки.
   `ModelDetailPage.dc.html`, `HomePage.dc.html` тощо — для наступних.
3. **`project/design_handoff_zx_spectrum/README.md`** — дизайн-токени з точними
   hex/типографікою/відступами. Дублюється у `src/styles/theme.css`.
4. **`chats/`** — стенограми дизайн-ітерацій (чому саме такі рішення).

> **Правило доступності (тверде, з ТЗ і дизайну):** контраст ≥ 4.5:1; `#0000FF`
> (blue) та `#00FF00` (green) **ніколи не текст на чорному** — лише заливка/рамка/
> іконка. Колір ніколи не єдиний носій сенсу. Дотримано в поточному коді — тримати
> й далі.

---

## 2. Стек і ключові рішення

| Пункт | Рішення | Примітка |
|---|---|---|
| Фреймворк | **Astro** (у сесії 5.18.2) | ⚠️ ТЗ вказує **Astro 7** (Rolldown/Sätteri) — його ще немає в npm. Код написано під архітектуру 7 (Content Collections, `astro/loaders`, i18n) — оновлення буде drop-in, коли 7 вийде. Див. розділ 6. |
| Node.js | ≥ 22.12 | зафіксовано в `.nvmrc` |
| Мова коду | TypeScript (strict) | `npm run check` = 0 помилок |
| Контент | Content Collections + Zod | `src/content.config.ts` валідує `src/data/models.json` на етапі збірки |
| i18n | вбудований Astro i18n | `uk` — без префікса, `en` — `/en/`; `prefixDefaultLocale:false` |
| Інтерактив | один клієнтський «острів» (vanilla `<script>`) | фільтри + компаратор; **без** JS-фреймворку — сторінка майже без JS |
| Зображення | `astro:assets` | авто-оптимізація у WebP (у білді — 4 фото × 3 розміри) |
| Стилі | scoped CSS у компонентах + токени в `theme.css` | без CSS-фреймворку |

**Свідомі спрощення відносно ТЗ (потребують уваги — розділ 7):**
- Дані зображень зберігаються як **один рядок `credit`** (як у прототипі), а не як
  структурований `images[]` з полями `license/author/source_url/accessed` (ТЗ §6/§7).
  Для виконання ТЗ це треба розширити + додати CI-перевірку ліцензій.
- Каталог містить **8 моделей** з дизайну, а не повний MVP-перелік на 20 (ТЗ §8.3).
  Схема вже це підтримує — треба лише дописати записи.

---

## 3. Структура проєкту (файл за файлом)

```
├── astro.config.mjs        i18n (uk/en), site URL, trailingSlash
├── tsconfig.json           strict; виключає project/ і dist/
├── .nvmrc                  22.12
├── package.json            залежність лише astro; devDeps: @astrojs/check, typescript
├── GETTING-STARTED.md      короткий старт (UA)
├── HANDOFF.md              цей документ
│
├── src/
│   ├── content.config.ts   ← схема моделей (Zod): enum ліцензій, confidence, bilingual
│   ├── data/models.json    ← ДАНІ 8 моделей (UA/EN). Головне місце для контент-правок
│   │
│   ├── i18n/ui.ts          ← усі рядки UI UA/EN + localizePath()/useTranslations()
│   │
│   ├── lib/
│   │   ├── catalog.ts      normalizeModels(), buildFilterGroups(), countryName(),
│   │   │                    deriveCommercialUse() (license → commercial_use, ТЗ §14)
│   │   └── images.ts       мапа ім'я-файлу → оптимізоване зображення (import.meta.glob)
│   │
│   ├── styles/theme.css    ← ТОКЕНИ дизайн-системи: палітра ZX, музейні navy/gold,
│   │                         типографіка, райдужна смуга, CRT-режим (+reduced-motion)
│   │
│   ├── layouts/
│   │   └── BaseLayout.astro <head>, шрифти, hreflang, інлайн-скрипт CRT (без спалаху)
│   │
│   ├── components/
│   │   ├── Header.astro     меню, активний пункт, UA/EN, CRT-тумблер, бургер (мобільний)
│   │   ├── Footer.astro     музейний футер (navy/gold), адреса, соцмережі, ліцензія
│   │   ├── ModelCard.astro  картка (з ExhibitCard): фото 4:3, бейджі, плашка ліцензії,
│   │   │                     стан «без фото», data-* атрибути для фільтрів
│   │   ├── CatalogView.astro ГОЛОВНИЙ блок: крихти, заголовок+лічильник, панель фільтрів,
│   │   │                     сітка, стан «нічого не знайдено», компаратор + скрипт-острів
│   │   └── RainbowBar.astro  фірмова смуга Sinclair
│   │
│   ├── assets/models/      *.jpg (оптимізуються). Зараз: zx-48k, zx-128, zx-plus, zx-plus3
│   │
│   └── pages/
│       ├── index.astro          мінімальна головна (UA) — заглушка, повний HomePage далі
│       ├── models/index.astro   каталог UA  → рендерить <CatalogView lang="uk">
│       └── en/
│           ├── index.astro       головна EN (заглушка)
│           └── models/index.astro каталог EN
│
├── public/
│   ├── fonts/Retroville_NC.ttf  піксельний заголовковий шрифт (self-hosted)
│   └── favicon.svg
│
├── project/                оригінальні дизайн-прототипи + design_handoff (референс)
└── chats/                  стенограми дизайн-сесій
```

---

## 4. Дизайн-система (швидкий довідник для нових сторінок)

Усі значення — у `src/styles/theme.css` як CSS-змінні. Використовувати їх, не хардкодити.

**Колір:** `--bg:#000` · `--text:#D7D7D7` · `--text-dim:#A8A8A8`.
ZX BRIGHT: `--zx-red/#FF0000`, `--zx-magenta/#FF00FF`, `--zx-cyan/#00FFFF`,
`--zx-yellow/#FFFF00`, `--zx-white/#FFFFFF` (можна текстом);
`--zx-blue/#0000FF`, `--zx-green/#00FF00` (**лише заливка/рамка**).
Музей: `--museum-navy/#152A52`, `--museum-gold/#FFD700` (службові зони).

**Типографіка:** `--font-pixel` (Retroville, заголовки/UI ≥16px, UPPERCASE) ·
`--font-body` (Golos Text, ≥18px, LH 1.6) · `--font-mono` (IBM Plex Mono, роки/метадані).

**Форма:** кути 0 (ретро); елевація рамками, не тінями; сітка контенту ~1180px;
райдужна смуга — акцент/роздільник (не шпалери). Рух 160–240ms ease-out;
поважати `prefers-reduced-motion`. Фокус — видима 2px рамка (blue).

**CRT-режим:** клас `html.crt`, стан у `localStorage['zx-crt']`, накладка
`.crt-overlay` у `BaseLayout`. За замовчуванням вимкнено.

---

## 5. Модель даних

`src/data/models.json` — масив, кожен запис валідовано схемою `src/content.config.ts`:

```jsonc
{
  "id": "zx-spectrum-48k",          // = slug, формує /models/{id}/
  "title": { "uk": "...", "en": "..." },
  "manufacturer": "Sinclair",
  "country": "GB",                   // enum GB|PT|US|PL → локалізована назва в UI
  "year": "1982",
  "ram": "48 KB",
  "ramKb": 48,                        // null = невідомо
  "desc": { "uk": "...", "en": "..." },
  "image": "zx-48k.jpg",             // null → картка «Шукаємо фотографію»
  "imageAlt": { "uk": "...", "en": "..." },
  "credit": "Bill Bertram · CC BY-SA 2.5 · Wikimedia",
  "inMuseum": true,
  "confidence": "high"               // high|medium|low; low → бейдж «⚠ уточнення»
}
```

**Фільтри** виводяться з даних автоматично (`buildFilterGroups`): рік · виробник ·
країна · обсяг ОЗП · наявність. Додаси нову модель — чипси оновляться самі.

---

## 6. Astro 5 → 7: що зміниться при апгрейді

Коли вийде Astro 7 (ТЗ §3), оновлення має бути мінімальним:
- `package.json`: `astro@^7`; Node лишається ≥ 22.12.
- Компілятор Rust суворіший до розмітки — поточна розмітка валідна (0 помилок check),
  але прогнати `npm run build` і виправити, якщо щось спливе.
- Content Collections / `file()`-loader / i18n API у коді вже в «новому» стилі —
  міграції не очікується.
- Перевірити ремарк/рехайп-плагіни (у нас їх немає → нічого робити).

Поки 7 немає — лишатися на 5.x. Нічого «майбутнього» в коді не використано.

---

## 7. Що НЕ зроблено — наступні кроки (за пріоритетом)

### A. Найважливіше — доробити ядро каталогу
1. **Структуровані ліцензії зображень + CI-перевірка** (ТЗ §6/§7.2). Замінити рядок
   `credit` на `images: [{ src, license (enum), author, source_url, accessed }]`;
   `commercial_use` виводити з `license` (є хелпер `deriveCommercialUse`). Додати
   скрипт у CI, що валить білд, якщо бракує будь-якого ліцензійного поля.
2. **Картка моделі `/models/{slug}/`** — «найважливіший шаблон» (ТЗ §5, дизайн
   `ModelDetailPage.dc.html`): hero, таблиця характеристик (null→«невідомо»),
   галерея з плашками, блок «В експозиції музею», «Джерела», навігація prev/next.
   Зараз картки каталогу ведуть на ці адреси, але сторінок ще немає (404).
3. **Повний перелік моделей** до MVP-20 (ТЗ §8.3): ZX80/81, +2A/+2B, Interface 1,
   ZX Printer тощо. Просто дописати в `models.json` (+ фото з чистою ліцензією).

### B. Решта сторінок (спільні компоненти вже готові до перевикористання)
4. **HomePage** (повна, зараз заглушка), **HistoryHub + таймлайн**, **Longread ×5**,
   **Clones**, **Software**, **Peripherals**, **Exhibits**, **Contacts** (Google Maps),
   **Sources/ліцензії**, **About**, **404** («R Tape loading error»).
   Дизайни всіх — у `project/*.dc.html`. Header уже має посилання на їхні адреси.

### C. Платформа / реліз (ТЗ §3, §9, §10)
5. **Пошук Pagefind** (статичний індекс на етапі збірки).
6. **SEO/OG:** `@astrojs/sitemap`, `robots.txt`, OG-зображення, RSS для лонгрідів,
   Schema.org (`Museum`/`Article`/`Product`). `hreflang` уже є в `BaseLayout`.
7. **Деплой:** Cloudflare Pages або GitHub Pages + GitHub Actions (PR→preview,
   merge→prod). DNS: CNAME `spectrum`. Захист гілки `main` + PR-чекліст (ТЗ §2).
8. **Редирект `/uk/* → /*`** (301) — на рівні хостингу.
9. **Аналітика** (Plausible — без cookie-банера) + перевірка Lighthouse ≥ 95/95.
10. **EN-контент**: перемикач працює, але EN живе лише для вже зроблених рядків;
    для сторінок без перекладу — банер «сторінка ще не перекладена» (ТЗ R5).

### D. Компаратор
11. Панель компаратора зараз відтворює дизайн (обрані моделі + зняття ×), але **без**
    механізму додавання з карток і без таблиці відмінностей. Доробити до повного
    (ТЗ §5.2: до 3 моделей, таблиця).

---

## 8. Відомі особливості / підводні камені

- **Google Fonts у пісочниці Claude:** у веб-сесії запити до fonts.googleapis.com
  блокуються сертифікатом — на скриншотах основний текст був системним шрифтом.
  **Локально й у проді шрифти вантажаться нормально.** (Retroville self-hosted —
  працює скрізь.) За бажанням — self-host і Golos Text/IBM Plex Mono для офлайну й швидкості.
- **Гарячі посилання на Wikimedia** були прибрані ще в дизайн-фазі; усі фото —
  локальні у `src/assets/`. Не повертати хотлінки (етикет + надійність).
- **«Мертві» пункти меню:** Історія/Клони/Софт/Периферія/Експонати/Контакти
  ведуть на ще не створені адреси (404). Це очікувано до збірки тих сторінок.
- **`project/` не збирається** Astro (поза `src/`), лишається як референс. У
  `tsconfig` виключено.

---

## 9. Перевірка якості (як переконатися, що нічого не зламалось)

```bash
npm run check    # типи: очікувано 0 errors
npm run build    # має завершитись "Complete!", 4 сторінки, оптимізовані webp
```

У цій сесії функціональність підтверджено e2e у браузері (25/25):
рендер 8 карток; фільтр Amstrad → «3 з 8 моделей»; комбінація Amstrad+Португалія →
стан «нічого не знайдено» + компаратор ховається; «Скинути» з обох кнопок; Португалія
→ лише Timex; фільтр «В музеї» → 3; зняття моделі з компаратора оновлює лічильник і
блокує кнопку <2; картки без фото; бейдж «⚠ уточнення» на +2A; фото у WebP; CRT
зберігається в localStorage; EN повністю локалізовано.

Для повторної e2e-перевірки локально — Playwright проти `npm run preview`
(скрипт у цю передачу не включено; за потреби напишу).

---

## 10. Продовження з Claude Code локально

1. Встановити Claude Code CLI, відкрити теку проєкту.
2. Дати агенту як контекст: **цей `HANDOFF.md`**, `TZ_spectrum_sncmuseum.md`,
   і потрібний прототип із `project/` (напр. `ModelDetailPage.dc.html`).
3. Типова перша задача: «Реалізуй сторінку картки моделі `/models/{slug}/` за
   `project/ModelDetailPage.dc.html`, перевикористовуючи Header/Footer/токени; дані
   бери з колекції `models`». Спільні компоненти й токени вже готові — нові сторінки
   мають наслідувати їх, а не дублювати стилі.

---

## 11. Стан Git

> Оновлено 2026-07-15 у локальній сесії. Попередня редакція цього розділу
> описувала стан у тимчасовому контейнері Claude Design; git-історія звідти не
> приїхала — бандл прибув як розпакований архів **без `.git`**.

Репозиторій ініціалізовано локально (`main`), історія — два коміти:

1. `Handoff-бандл з Claude Design + ТЗ замовника` — стан «як отримано»
   (`project/`, `chats/`, `README.md`, ТЗ)
2. `Каталог моделей /models/ на Astro (UA/EN)` — уся реалізація каталогу

Додано інфраструктуру ТЗ §2:
- `.github/pull_request_template.md` — чек-лист (факти · джерела · ліцензії ·
  `confidence` · затвердження музеєм)
- `.github/workflows/ci.yml` — `npm run check` + `npm run build` на кожен PR;
  Node-версія береться з `.nvmrc`

**Ще не зроблено:**
- **Remote відсутній** — репозиторій лише локальний, поза бекапом. Створити
  репозиторій на GitHub і запушити.
- **Захист `main`** (ТЗ §2: заборона прямого push, обов'язкове ревʼю ≥ 1
  відповідальної особи) — налаштовується в GitHub після появи remote.
- **CI ще не проганявся** — воркфлоу написано, але не перевірено жодним запуском
  (локально бракує Node, див. §12).

---

## 12. Локальне середовище

**Node.js на робочій машині не встановлено** (станом на 2026-07-15: немає ні
`node`, ні nvm, ні Homebrew). Поки його немає — `npm install`, `npm run dev`,
`npm run check` і `npm run build` не запускаються, тобто **жодна зміна коду не
перевіряється локально**. Це перше, що треба зняти.

Потрібно: **Node.js ≥ 22.12** (зафіксовано в `.nvmrc`; вимога ТЗ §3).

Два шляхи:

1. **Інсталятор** — [nodejs.org](https://nodejs.org/) → LTS для macOS (`.pkg`).
   Найпростіше, якщо потрібна одна версія Node.
2. **nvm** — якщо колись знадобляться різні версії під різні проєкти:
   ```bash
   curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
   # перезапустити термінал, далі в теці проєкту:
   nvm install    # прочитає .nvmrc
   nvm use
   ```

Після встановлення — перевірка, що все живе:

```bash
node -v          # має бути ≥ v22.12
npm install
npm run check    # очікувано: 0 помилок
npm run build    # очікувано: "Complete!"
npm run dev      # → http://localhost:4321/models/
```
